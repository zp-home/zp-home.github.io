{
  "name": "zp-home.github.io",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages --nojekyll -m \"deploy: publish site\""
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@fontsource-variable/dm-sans": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "5.3.0",
    "astro": "7.2.4",
    "lucide-astro": "0.556.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.6",
    "gh-pages": "6.3.0",
    "typescript": "5.9.2"
  }
}
